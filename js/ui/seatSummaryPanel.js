/**
 * Seat summary rendering is handled within licensePanel.js since the seat
 * summary section is part of the left panel. This module exists as a named
 * placeholder per the plan. The seat-summary-updated event is subscribed
 * to in licensePanel.js.
 */

// No additional exports needed — seat summary rendering lives in licensePanel.js.
